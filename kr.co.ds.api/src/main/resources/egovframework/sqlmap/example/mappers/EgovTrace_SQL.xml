<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.trace.service.impl.TraceiMapper">
	
	<select id="selectTraceList" parameterType="fSearchEntity" resultType="unsongEntity" >

		SELECT    billNo
				, input_date
				, input_time
				, chasu
				, magam_gb
				, transCode
				, terminal_sunbun
				, delivery_yn
				, sendingAgencycode
				, arrivalAgencycode
				, lineSerialno
				, sendingManTel
				, sendingMan 
				, arrivalManTel
				, arrivalMan
				, zipCode
				, adress
				, preFare
				, fare
				, deliveryFare
				, ogideliveryFare
				, wayfare
				, enterCharge
				, workCharge
				, distance
				, payway
				, goods
				, pojang
				, qty
				, weight
				, memo
				, ganChasu
				, hachaChasu
				, billState
				, changgo
				, nearGubun
				, update_date
				, creator
				, updator
				, dmlGubun
				, billGubun
				, deliveryCourse
				, barcode_yn
				, creatDate
				, barcodeNo
				, createAgency
			  FROM unsong
			 WHERE input_date between #{searchStartDt} and #{searchEndDt}
			 <if test="searchKeyword != null and searchKeyword != ''">
		        <choose>
		            <when test="searchCondition == 0">
						AND ARRIVALMANTEL = #{searchKeyword}
					</when>
		            <when test="searchCondition == 1">
						AND SENDINGMANTEL = #{searchKeyword}
					</when>
				</choose>
			</if>
	</select>
</mapper>